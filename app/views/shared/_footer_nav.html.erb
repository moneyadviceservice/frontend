<nav class="footer-nav" role="navigation" aria-label="" tabindex="-1">
  <h2 id="footer-menu" class="footer-nav__header">Menu</h2>

  <ul class="footer-nav__categories" role="" aria-label="">
    <% clumps.each do |clump| %>
      <% clump.categories.each do |category| %>
        <li class="footer-nav__category">
          <%= link_to category.title, main_app.category_path(category.id), role: '', class: 'footer-nav__link' %>
        </li>
      <% end %>
    <% end %>
  </ul>
</nav>
