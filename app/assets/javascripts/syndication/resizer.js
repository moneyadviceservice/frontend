// Generated by CoffeeScript 1.12.7
(function() {
  window.handleMASResizeMessage = function(event) {
    var error, height, iframe, parts, prefix, data, toolID;

    data = event.data;

    try {
      if (typeof event.data === "string") {
        parts = event.data.split("-");
        prefix = parts[0];
        height = parseInt(parts[1]);
        toolID = parts[2] + '-iframe';

        if (prefix === "MASRESIZE") {
          iframe = document.querySelector('#' + toolID);
          iframe.style.height = height + "px";
        }
      }
    } catch (error1) {
      error = error1;
    }
  };

  if (window.addEventListener) {
    addEventListener("message", window.handleMASResizeMessage, false);
  } else {
    attachEvent("onmessage", window.handleMASResizeMessage);
  }
}).call(this);
