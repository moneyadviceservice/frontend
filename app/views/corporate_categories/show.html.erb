<% set_meta_tags(title:       corporate_category.title,
                 description: category.description,
                 canonical:   category.canonical_url,
                 alternate:   category.alternate_options) %>

<% content_for(:context_bar) do %>
  <%= render 'shared/breadcrumbs', breadcrumbs: @breadcrumbs unless @breadcrumbs.empty? %>
<% end %>

<div class="l-main editorial">
  <div class="l-main__row">
    <main class="l-main__cell-double" role="main">
      <%= heading_tag category.title %>

      <% unless category.third_level_navigation? %>
        <p class="intro"><%= category.description %></p>
      <% end %>

      <%= category.render_contents %>

      <% if category.third_level_navigation? %>
        <%= article.content %>
      <% end %>
    </main>
  </div>
</div>

<div class="l-nav">
  <%= render 'shared/mas_sections_link' if category_within_corporate_section?(category) %>
  <%= render 'shared/nav',
             categories:        corporate_category_navigation,
             active_categories: active_categories %>
</div>
